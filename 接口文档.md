# MindMOOC 业务流程

## API 列表

### 🔐 用户模块（不需认证）

- `POST /api/users/register` - 用户注册
- `POST /api/users/login` - 用户登录
- `POST /api/users/password/reset-code` - 发送重置密码验证码
- `POST /api/users/password/reset` - 验证码校验并重置密码

### 👤 用户模块（需认证）

- `GET /api/users/me` - 获取当前用户信息
- `PUT /api/users/me` - 更新用户信息
- `DELETE /api/users/me` - 注销当前账户
- `GET /api/users/{userId}` - 获取指定用户信息

### 🎬 视频模块

- `POST /api/videos/upload/init` - 初始化上传（申请 MinIO PUT 预签名 URL）
- `POST /api/videos/upload/complete` - 上传完成确认并入库
- `GET /api/videos/{videoId}/play-url` - 获取播放预签名 URL（2小时）
- `GET /api/videos/{videoId}` - 获取视频信息
- `GET /api/videos/list?pageNum=1&pageSize=10` - 获取视频列表
- `DELETE /api/videos/{videoId}` - 删除视频

### 📝 任务模块

- `POST /api/tasks` - 创建任务
- `GET /api/tasks/{taskId}` - 获取任务详情
- `GET /api/tasks/{taskId}/status` - 获取任务状态
- `GET /api/tasks/my?pageNum=1&pageSize=10` - 获取我的任务列表（详细版任务列表）
- `GET /api/tasks/my/status?pageNum=1&pageSize=10` - 获取我的任务状态列表（轻量版任务列表，一般够用）
- `PUT /api/tasks/{taskId}/title` - 修改任务标题
- `DELETE /api/tasks/{taskId}` - 删除任务

### 🗺️ 思维导图模块

- `GET /api/mindmaps/{mindmapId}` - 获取思维导图
- `GET /api/mindmaps/task/{taskId}` - 根据任务ID获取思维导图
- `GET /api/mindmaps/my?pageNum=1&pageSize=10` - 获取我的思维导图列表
- `GET /api/mindmaps/{mindmapId}/nodes` - 获取节点列表（树形结构）
- `POST /api/mindmapNodes/{mindmapId}/nodes` - 添加节点
- `PUT /api/mindmapNodes/nodes/{nodeId}` - 更新节点
- `DELETE /api/mindmapNodes/nodes/{nodeId}` - 删除节点
- `POST /api/mindmapNodes/{mindmapId}/nodes/batch-sync` - 全量同步节点（后端自动判定增删改）
- `GET /api/mindmapNodes/nodes/{nodeId}/chart-url` - 获取 chart 节点图片访问地址
- `POST /api/mindmaps/{mindmapId}/regenerate` - 重新生成 Mermaid 代码

### 💬 反馈模块

- `POST /api/feedback` - 提交反馈
- `GET /api/feedback/{feedbackId}` - 获取反馈详情
- `GET /api/feedback/my?pageNum=1&pageSize=10` - 获取我的反馈列表
- `GET /api/feedback/all?pageNum=1&pageSize=10&status=open` - 获取所有反馈（管理员）
- `PUT /api/feedback/{feedbackId}/status?status=closed` - 更新反馈状态（管理员）

### 🔧 内部接口（由 Flask AI 服务调用）

- （已废弃）`POST /api/internal/tasks/callback` - 历史回调接口，当前流程不再依赖
  
  

****

## 完整业务流程示例

### 场景：用户上传视频并生成思维导图

#### 步骤 1：用户注册/登录

```bash
# 登录
curl -X POST http://localhost/api/users/login \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser","password":"123456"}'

# 保存返回的 token
TOKEN="eyJhbGciOiJIUzI1NiJ9..."
```

#### 步骤 2：上传视频

```bash
curl -X POST http://localhost/api/videos/upload/init \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "filename": "demo.mp4",
    "mimeType": "video/mp4",
    "videoTitle": "我的慕课视频",
    "fileHash": "sha256-xxx",
    "fileSize": 123456789,
    "duration": 95.321
  }'

# 前端拿到 uploadUrl 后直接 PUT 到 MinIO
# 上传成功后再调用 complete
curl -X POST http://localhost/api/videos/upload/complete \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "uploadId": "upload-xxx",
    "objectKey": "videos/2026/04/14/xxx.mp4"
  }'

# 保存返回的 videoId
VIDEO_ID="video-id-xxx"
```

#### 步骤 3：创建任务

```bash
curl -X POST http://localhost/api/tasks \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d "{\"videoId\":\"$VIDEO_ID\",\"taskType\":\"common\"}"

# 保存返回的 taskId
TASK_ID="task-id-xxx"
```

#### 步骤 4：查询任务状态

```bash
curl -X GET http://localhost/api/tasks/$TASK_ID \
  -H "Authorization: Bearer $TOKEN"

# 任务状态：pending -> processing -> completed
```

#### 步骤 5：获取思维导图

```bash
# 任务完成后，根据任务ID获取思维导图
curl -X GET http://localhost/api/mindmaps/task/$TASK_ID \
  -H "Authorization: Bearer $TOKEN"

# 保存返回的 mindmapId
MINDMAP_ID="mindmap-id-xxx"
```

#### 步骤 6：修改思维导图节点

```bash
# 添加节点
curl -X POST http://localhost/api/mindmapNodes/$MINDMAP_ID/nodes \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"parentId":"parent-node-id","content":"新节点内容","startTime":10,"endTime":20,"nodeOrder":1}'

# 更新节点
curl -X PUT http://localhost/api/mindmapNodes/nodes/node-id-xxx \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"content":"更新后的节点内容"}'

# 删除节点
curl -X DELETE http://localhost/api/mindmapNodes/nodes/node-id-xxx \
  -H "Authorization: Bearer $TOKEN"
```

#### 步骤 7：重新生成 Mermaid 代码

```bash
curl -X POST http://localhost/api/mindmaps/$MINDMAP_ID/regenerate \
  -H "Authorization: Bearer $TOKEN"
```



****

## business-service 接口详细说明

### 通用说明

- **统一响应结构 `Result<T>`**
  
  - 所有业务接口（除 `/api/test`）统一返回：
    
    ```json
    {
      "code": 200,
      "message": "操作成功",
      "data": {}
    }
    ```
  
  - **code**: `number`，`200` 表示成功，其它为错误。
  
  - **message**: `string`，提示信息。
  
  - **data**: `any`，具体业务数据或 `null`。

- **分页返回结构 `IPage<T>`（MyBatis-Plus）**
  
  - 所有分页接口 `data` 结构类似：
    
    ```json
    {
      "records": [],
      "total": 100,
      "size": 10,
      "current": 1,
      "pages": 10
    }
    ```

- **认证与用户标识**
  
  - 前端只需在网关层携带：`Authorization: Bearer <token>`。
  - 网关校验通过后会向业务服务转发请求，并自动注入：`X-User-Id: <用户ID>`。

---

### 数据模型（DTO / VO）

- **RegisterRequest（注册请求体）**
  
  - **username**: `string`，必填，3–50 字符。
  - **email**: `string`，必填，邮箱格式。
  - **password**: `string`，必填，6–20 字符。

- **LoginRequest（登录请求体）**
  
  - **username**: `string`，必填，用户名或邮箱。
  - **password**: `string`，必填。

- **UpdateUserRequest（更新用户信息）**
  
  - **username**: `string`，可选，新用户名。
  - **email**: `string`，可选，新邮箱。
  - **avatarUrl**: `string`，可选，新头像 URL。

- **SendResetCodeRequest（发送重置密码验证码请求体）**
  
  - **email**: `string`，必填，邮箱格式。

- **ResetPasswordRequest（验证码重置密码请求体）**
  
  - **email**: `string`，必填，邮箱格式。
  - **code**: `string`，必填，6位验证码。
  - **newPassword**: `string`，必填，6-20 字符。

- **DeleteAccountRequest（注销账户请求体）**
  
  - **password**: `string`，必填，当前密码（用于二次确认）。

- **UserVO（用户信息返回）**
  
  - **id**: `string`，用户ID。
  - **username**: `string`。
  - **email**: `string`。
  - **avatarUrl**: `string`。
  - **role**: `number`，`0=普通用户`，`1=管理员`。
  - **createdAt**: `string`，时间戳。

- **LoginResponse（登录响应）**
  
  - **token**: `string`，JWT Token。
  - **user**: `UserVO`。

- **VideoVO（视频信息返回）**
  
  - **id**: `string`，视频ID。
  - **originalFilename**: `string`，原始文件名。
  - **videoTitle**: `string`，视频标题。
  - **duration**: `number`，时长（秒）。
  - **fileSize**: `number`，大小（字节）。
  - **mimeType**: `string`，MIME 类型。
  - **storageUrl**: `string`，访问 URL。
  - **summary**: `string`，视频概要。
  - **createdAt**: `string`。

- **VideoUploadInitRequest（初始化上传请求体）**

  - **filename**: `string`，必填，原始文件名。
  - **mimeType**: `string`，必填，MIME 类型（如 `video/mp4`）。
  - **videoTitle**: `string`，可选，视频标题。
  - **fileHash**: `string`，必填，前端计算的 SHA-256。
  - **fileSize**: `number`，必填，文件大小（字节）。
  - **duration**: `number`，必填，视频时长（秒，整数）。

- **VideoUploadInitResponse（初始化上传响应体）**

  - **uploadId**: `string`，上传会话ID。
  - **objectKey**: `string`，MinIO 对象键。
  - **uploadUrl**: `string`，MinIO PUT 预签名 URL。
  - **expireAt**: `string`，预签名 URL 过期时间。
  - **duplicated**: `boolean`，是否命中去重。
  - **videoId**: `string`，命中去重时直接返回已有视频ID。

- **VideoUploadCompleteRequest（上传完成确认请求体）**

  - **uploadId**: `string`，必填，上传会话ID。
  - **objectKey**: `string`，必填，MinIO 对象键。

- **VideoPlayUrlVO（播放地址响应体）**

  - **videoId**: `string`，视频ID。
  - **playUrl**: `string`，MinIO GET 预签名 URL。
  - **expireAt**: `string`，URL 过期时间（默认 2 小时）。

- **CreateTaskRequest（创建任务请求体）**
  
  - **videoId**: `string`，必填，视频ID。
  - **title**: `string`，必填，任务名称。
  - **requirement**: `string`，可选，任务要求。
  - **taskType**: `string`，可选，默认 `"common"`。

- **UpdateTaskTitleRequest（更新任务标题请求体）**
  
  - **title**: `string`，必填，任务标题，最长 255 字符。

- **TaskVO（任务返回）**
  
  - **id**: `string`，任务ID。
  - **userId**: `string`。
  - **videoId**: `string`。
  - **video**: `VideoVO`。
  - **title**: `string`。
  - **requirement**: `string`。
  - **status**: `string`，`pending / processing / completed / failed` 等。
  - **taskType**: `string`。
  - **errorDetails**: `string`，错误详情。
  - **createdAt / startedAt / completedAt**: `string`。

- **TaskStatusVO（任务状态返回）**
  
  - **id**: `string`，任务ID。
  - **title**: `string`，任务标题。
  - **status**: `string`，任务状态（`pending / processing / completed / failed`）。
  - **errorDetails**: `string`，错误详情（失败时有值）。
  - **startedAt / completedAt**: `string`。

- **CreateFeedbackRequest（创建反馈请求体）**
  
  - **type**: `string`，必填，反馈类型（如 `bug / suggestion / other`）。
  - **details**: `string`，必填，反馈内容。

- **FeedbackVO（反馈返回）**
  
  - **id**: `string`。
  - **userId**: `string`。
  - **type**: `string`。
  - **details**: `string`。
  - **status**: `string`。
  - **createdAt**: `string`。

- **UpdateNodeRequest（新增/更新节点请求体）**
  
  - **parentId**: `string`，父节点ID，可选。
  - **content**: `string`，节点内容，可选但推荐填写。
  - **nodeType**: `string`，节点类型（`text / formula / chart`）。
  - **chartUrl**: `string`，图表节点图片URL。
  - **startTime**: `number`，起始时间（秒，支持小数）。
  - **endTime**: `number`，结束时间（秒，支持小数）。
  - **nodeOrder**: `number`，节点顺序。

- **MindmapNodeUpsertRequest（节点全量同步中的单节点请求体）**

  - **id**: `string`，必填，节点ID（前端生成 UUID）。
  - **parentId**: `string`，可选，父节点ID。
  - **nodeOrder**: `number`，必填，节点顺序。
  - **content**: `string`，必填，节点内容。
  - **nodeType**: `string`，必填，节点类型（`text / formula / chart`）。
  - **chartUrl**: `string`，可选，图表节点图片对象键。
  - **startTime**: `number`，可选，起始时间（秒，支持小数）。
  - **endTime**: `number`，可选，结束时间（秒，支持小数）。

- **MindmapBatchSyncRequest（节点全量同步请求体）**

  - **nodes**: `MindmapNodeUpsertRequest[]`，必填，修改后的完整节点列表（不能为空）。

- **MindmapBatchSyncResultVO（节点全量同步结果）**

  - **addedCount**: `number`，新增节点数。
  - **updatedCount**: `number`，更新节点数。
  - **deletedCount**: `number`，删除节点数。
  - **totalCount**: `number`，同步后节点总数。

- **NodeChartUrlVO（图表节点图片访问地址）**

  - **nodeId**: `string`，节点ID。
  - **url**: `string`，图片访问 URL（GET 预签名）。
  - **expireAt**: `string`，URL 过期时间。

- **NodeVO（思维导图节点返回）**
  
  - **id**: `string`，节点ID。
  - **mapId**: `string`，导图ID。
  - **parentId**: `string`。
  - **content**: `string`。
  - **nodeType**: `string`，节点类型。
  - **chartUrl**: `string`，图表节点图片URL。
  - **startTime / endTime**: `number`（支持小数）。
  - **nodeOrder**: `number`。
  - **children**: `NodeVO[]`，子节点。
  - **createdAt / updatedAt**: `string`。

- **MindmapVO（思维导图返回）**
  
  - **id**: `string`，导图ID。
  - **userId**: `string`。
  - **videoId**: `string`。
  - **video**: `VideoVO`。
  - **taskId**: `string`。
  - **title**: `string`。
  - **mermaidCode**: `string`，Mermaid 代码。
  - **nodes**: `NodeVO[]`。
  - **createdAt / updatedAt**: `string`。

- **AiCallbackRequest（AI 回调请求体）**
  
  - **taskId**: `string`。
  - **status**: `string`，`success` 或 `failed`。
  - **mermaidCode**: `string`。
  - **summary**: `string`。
  - **errorMessage**: `string`。
  - **originalOutput**: `string`，JSON 字符串。

---

### Controller 接口

#### 1. UserController（`/api/users`）

- **POST /api/users/register** — 用户注册  
  
  - 请求头：`Content-Type: application/json`  
  
  - 请求体：`RegisterRequest`  
    
    ```json
    {
      "username": "alice",
      "email": "alice@example.com",
      "password": "123456"
    }
    ```
  
  - 返回：`Result<UserVO>`

- **POST /api/users/login** — 用户登录  
  
  - 请求头：`Content-Type: application/json`  
  - 请求体：`LoginRequest`  
  - 返回：`Result<LoginResponse>`（含 `token` 和 `user`）

- **GET /api/users/me** — 获取当前用户信息  
  
  - 请求头：`Authorization: Bearer <token>`（经网关转为 `X-User-Id`）  
  - 返回：`Result<UserVO>`

- **GET /api/users/{userId}** — 获取指定用户信息  
  
  - 路径参数：`userId: string`  
  - 返回：`Result<UserVO>`

- **PUT /api/users/me** — 更新当前用户信息  
  
  - 请求头：`Authorization: Bearer <token>`，`Content-Type: application/json`  
  - 请求体：`UpdateUserRequest`  
  - 返回：`Result<UserVO>`

- **POST /api/users/password/reset-code** — 发送忘记密码验证码  
  
  - 请求头：`Content-Type: application/json`  
  - 请求体：`SendResetCodeRequest`  
  - 返回：`Result<Void>`

- **POST /api/users/password/reset** — 验证码校验并重置密码  
  
  - 请求头：`Content-Type: application/json`  
  - 请求体：`ResetPasswordRequest`  
  - 返回：`Result<Void>`

- **DELETE /api/users/me** — 注销当前账户  
  
  - 请求头：`Authorization: Bearer <token>`，`Content-Type: application/json`  
  - 请求体：`DeleteAccountRequest`  
  - 返回：`Result<Void>`

#### 2. VideoController（`/api/videos`）

- **POST /api/videos/upload/init** — 初始化上传（申请 MinIO PUT 预签名 URL）  

  - 请求头：`Authorization: Bearer <token>`，`Content-Type: application/json`  
  - 请求体：`VideoUploadInitRequest`  
  - 返回：`Result<VideoUploadInitResponse>`

- **POST /api/videos/upload/complete** — 上传完成确认并入库  

  - 请求头：`Authorization: Bearer <token>`，`Content-Type: application/json`  
  - 请求体：`VideoUploadCompleteRequest`  
  - 返回：`Result<VideoVO>`

- **GET /api/videos/{videoId}/play-url** — 获取播放预签名 URL（2小时）  

  - 请求头：`Authorization: Bearer <token>`  
  - 路径参数：`videoId: string`  
  - 返回：`Result<VideoPlayUrlVO>`

- **GET /api/videos/{videoId}** — 获取视频信息  
  
  - 路径参数：`videoId: string`  
  - 返回：`Result<VideoVO>`

- **GET /api/videos/list** — 获取视频列表（分页）  
  
  - 查询参数：`pageNum`（默认 1），`pageSize`（默认 10）  
  - 返回：`Result<IPage<VideoVO>>`

- **DELETE /api/videos/{videoId}** — 删除视频  
  
  - 路径参数：`videoId: string`  
  - 返回：`Result<Void>`

#### 3. TaskController（`/api/tasks`）

- **POST /api/tasks** — 创建任务  
  
  - 请求头：`Authorization: Bearer <token>`，`Content-Type: application/json`  
  
  - 请求体：`CreateTaskRequest`  
    
    ```json
    {
      "videoId": "video-123",
      "title": "学习任务1",
      "requirement": "请生成思维导图",
      "taskType": "common"
    }
    ```
  
  - 返回：`Result<TaskVO>`

- **GET /api/tasks/{taskId}** — 获取任务详情  
  
  - 路径参数：`taskId: string`  
  - 返回：`Result<TaskVO>`

- **DELETE /api/tasks/{taskId}** — 删除任务

  - 路径参数：`taskId: string`
  - 返回：`Result<Void>`

- **GET /api/tasks/{taskId}/status** — 获取任务状态 
  
  - 请求头：`Authorization: Bearer <token>`  
  - 路径参数：`taskId: string`  
  - 返回：`Result<TaskStatusVO>`  
  - 说明：返回单个任务的轻量状态信息，包含任务ID、标题、状态、错误详情、开始时间和完成时间。

- **GET /api/tasks/my** — 获取当前用户任务列表  
  
  - 请求头：`Authorization: Bearer <token>`  
  - 查询参数：`pageNum`，`pageSize`  
  - 返回：`Result<IPage<TaskVO>>`

- **GET /api/tasks/my/status** — 获取当前用户任务状态列表  
  
  - 请求头：`Authorization: Bearer <token>`  
  - 查询参数：`pageNum`，`pageSize`  
  - 返回：`Result<IPage<TaskStatusVO>>`  
  - 说明：适合前端任务列表页只展示状态相关信息时使用。

- **PUT /api/tasks/{taskId}/title** — 修改任务标题  
  
  - 请求头：`Authorization: Bearer <token>`，`Content-Type: application/json`  
  - 路径参数：`taskId: string`  
  - 请求体：`UpdateTaskTitleRequest`  
  - 返回：`Result<TaskVO>`

#### 4. MindmapController（`/api/mindmaps`）

- **GET /api/mindmaps/{mindmapId}** — 根据导图ID获取思维导图  
  
  - 路径参数：`mindmapId: string`  
  - 返回：`Result<MindmapVO>`

- **GET /api/mindmaps/task/{taskId}** — 根据任务ID获取思维导图  
  
  - 路径参数：`taskId: string`  
  - 返回：`Result<MindmapVO>`

- **GET /api/mindmaps/my** — 获取当前用户思维导图列表  
  
  - 请求头：`Authorization: Bearer <token>`  
  - 查询参数：`pageNum`，`pageSize`  
  - 返回：`Result<IPage<MindmapVO>>`

- **GET /api/mindmaps/{mindmapId}/nodes** — 获取导图节点树  
  
  - 路径参数：`mindmapId: string`  
  - 返回：`Result<NodeVO[]>`

- **POST /api/mindmaps/{mindmapId}/regenerate** — 重新生成 Mermaid 代码  
  
  - 路径参数：`mindmapId: string`  
  - 返回：`Result<string>`（`data` 为新的 Mermaid 代码）

#### 5. MindmapNodeController（`/api/mindmapNodes`）

- **POST /api/mindmapNodes/{mindmapId}/nodes** — 添加节点  
  
  - 路径参数：`mindmapId: string`  
  - 请求体：`UpdateNodeRequest`  
  - 返回：`Result<NodeVO>`

- **PUT /api/mindmapNodes/nodes/{nodeId}** — 更新节点  
  
  - 路径参数：`nodeId: string`  
  - 请求体：`UpdateNodeRequest`  
  - 返回：`Result<NodeVO>`

- **DELETE /api/mindmapNodes/nodes/{nodeId}** — 删除节点及其子节点  
  
  - 路径参数：`nodeId: string`  
  - 返回：`Result<Void>`

- **POST /api/mindmapNodes/{mindmapId}/nodes/batch-sync** — 全量同步节点（后端自动判定增删改）

  - 请求头：`Authorization: Bearer <token>`
  - 路径参数：`mindmapId: string`
  - 请求体：`MindmapBatchSyncRequest`
  - 返回：`Result<MindmapBatchSyncResultVO>`

- **GET /api/mindmapNodes/nodes/{nodeId}/chart-url** — 获取 chart 节点图片访问地址

  - 请求头：`Authorization: Bearer <token>`
  - 路径参数：`nodeId: string`
  - 返回：`Result<NodeChartUrlVO>`

#### 6. FeedbackController（`/api/feedback`）

- **POST /api/feedback** — 创建反馈  
  
  - 请求头：`Authorization: Bearer <token>`，`Content-Type: application/json`  
  
  - 请求体：`CreateFeedbackRequest`  
    
    ```json
    {
      "type": "bug",
      "details": "课程播放到 1:23 会卡死"
    }
    ```
  
  - 返回：`Result<FeedbackVO>`

- **GET /api/feedback/{feedbackId}** — 获取反馈详情  
  
  - 路径参数：`feedbackId: string`  
  - 返回：`Result<FeedbackVO>`

- **GET /api/feedback/my** — 获取当前用户反馈列表  
  
  - 请求头：`Authorization: Bearer <token>`  
  - 查询参数：`pageNum`，`pageSize`  
  - 返回：`Result<IPage<FeedbackVO>>`

- **GET /api/feedback/all** — 获取所有反馈列表（管理员）  
  
  - 查询参数：`pageNum`，`pageSize`，`status`（可选）  
  - 返回：`Result<IPage<FeedbackVO>>`

- **PUT /api/feedback/{feedbackId}/status** — 更新反馈状态（管理员）  
  
  - 路径参数：`feedbackId: string`  
  - 查询参数：`status: string`  
  - 返回：`Result<FeedbackVO>`

#### 7. InternalController（`/api/internal`）

- **POST /api/internal/tasks/callback** — AI 服务回调接口  
  - 请求头：`Content-Type: application/json`  
  - 请求体：`AiCallbackRequest`  
  - 返回：`Result<Void>`（成功时 `data = null`）  
  - 用于 AI 服务异步回传任务结果、Mermaid 代码和节点信息，前端不调用。

> 当前新流程说明：创建任务时，business-service 会先创建 `task + mindmap`，随后调用 AI 服务并传递`user_id, video_id, task_id, mindmap_id, instruction`，AI 服务直接写入数据库，不再依赖回调接口。

#### 8. TestController（`/api`）

- **GET /api/test** — 测试接口  
  - 无需认证，直接返回字符串 `"测试成功"`（不包裹在 `Result` 中）。
